# libsort
C++ library for sorting arrays

Libsort inlcude:
- O(n log n): QuickSort, HeapSort, IntroSort, ShellSort
- O(n^2): DualSort, InsertionSort, Selection, BubbleSort
- O(n+k): CountingSort(Numbers only)


## TODO:
- RadixSort
- Cocktail Shaker Sort
- BinInsertionSort
- etc
